#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
run_cmd="$repo_root/scripts/harness/run-cmd"

"$run_cmd" --label "doc-lint" --error-pattern "^doc-lint:" -- "$repo_root/scripts/harness/doc-lint"

"$repo_root/scripts/harness/mvn" -q compile "$@"
