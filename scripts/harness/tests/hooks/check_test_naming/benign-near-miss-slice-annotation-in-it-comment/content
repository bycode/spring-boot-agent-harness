package nl.jinsoo.template.notepad;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Historical note: this IT used to be split into slice tests. Do not
 * re-introduce @DataJdbcTest, @WebMvcTest, @RestClientTest, or
 * @ApplicationModuleTest here — slice annotations belong in *Test.java.
 */
// Previously: @WebMvcTest — produced bug, see NOTE-456.
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
class NoteEndpointIT {

    @Test
    void getNote() {
        // noop
    }
}
