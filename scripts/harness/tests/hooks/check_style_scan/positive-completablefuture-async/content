package nl.jinsoo.template.notepad;

import java.util.concurrent.CompletableFuture;

public class NoteAsync {
    public CompletableFuture<String> compute() {
        return CompletableFuture.supplyAsync(() -> "hello");
    }
}
